# Pinned to the minor series each was developed and tested against. `~=` allows
# patch releases (security fixes) but not a minor bump that could change
# behaviour without anyone noticing.
fastapi~=0.135.1
uvicorn~=0.41.0
requests~=2.32.5
